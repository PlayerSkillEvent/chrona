package dev.chrona.common.region;

/**
 * Enum representing different types of regions.
 * Rect gets defined by two corner points, and
 * internally converted to a polygon.
 */
public enum RegionType {
    POLYGON,
    RECT
}
